{
    "version": 2,
    "builds": [
        {
            "src": "*.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/v1/recipe/(.*)",
            "dest": "/app.ts"
        }
    ]
    
}
