{
    "version": 2,
    "builds": [
        {
            "src": "*.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/backend/(.*)",
            "dest": "/backend/app.ts"
        }
    ]
    
}
